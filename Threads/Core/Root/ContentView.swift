import SwiftUI

struct ContentView: View {
  var body: some View {
    NavigationStack {
      VStack {
        Text("Hello, World!")
        Spacer()
        Text("Hello, World!")
        Spacer()
        Text("Hello, World!")
      }
    }
  }
}

#Preview {
  ContentView()
}
